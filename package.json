{
  "name": "@binary-owls/sdk-node",
  "version": "2.0.1",
  "description": "JavaScript helpers with TypeScript",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "license": "MIT",
  "scripts": {
    "prebuild": "pnpm test",
    "build": "rimraf lib/ && tsc",
    "postpub": "echo 'Publish succeed, but YOU MUST NOW COMMIT CHANGES!'",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^25.2.3",
    "@types/node": "^20.11.19",
    "jest": "^26.0.1",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.0.0",
    "typescript": "^5.3.3"
  }
}
